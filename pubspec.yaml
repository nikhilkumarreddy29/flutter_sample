name: first_flutter_vs_code
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=3.2.6 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^5.0.1+1
  camera: ^0.5.8+8
  url_launcher: ^6.2.4
  pandabar: ^0.1.2  
  call_log: ^4.0.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
flutter_phone_direct_caller: ^2.1.1
